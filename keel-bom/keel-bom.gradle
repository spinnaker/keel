plugins {
  id "java-platform"
}

javaPlatform {
  allowDependencies()
}

dependencies {
  api(platform("com.netflix.spinnaker.kork.internal:kork-bom"))
  constraints {
    rootProject
      .subprojects
      .stream()
      .filter { it != project }
      .forEach { api(project(it.path)) }
  }
}
